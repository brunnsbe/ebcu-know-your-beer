{#
  Callout Component
  Usage: {% set calloutType = "info" %}{% set calloutTitle = "Title" %}{% set calloutContent = "Content" %}{% include "components/callout.njk" %}
  Types: info, warning, tip, note
#}
{% set bgColor = "bg-slate-50 border-slate-200" %}
{% set iconColor = "text-slate-500" %}

{% if calloutType == "warning" %}
  {% set bgColor = "bg-amber-50 border-amber-200" %}
  {% set iconColor = "text-amber-600" %}
{% elif calloutType == "tip" %}
  {% set bgColor = "bg-green-50 border-green-200" %}
  {% set iconColor = "text-green-600" %}
{% elif calloutType == "note" %}
  {% set bgColor = "bg-blue-50 border-blue-200" %}
  {% set iconColor = "text-blue-600" %}
{% endif %}

<div class="rounded-lg border {{ bgColor }} p-4 my-6">
  <div class="flex gap-3">
    <div class="flex-shrink-0 {{ iconColor }}">
      {% if calloutType == "warning" %}
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
      {% elif calloutType == "tip" %}
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path></svg>
      {% elif calloutType == "note" %}
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
      {% else %}
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
      {% endif %}
    </div>
    <div>
      {% if calloutTitle %}
      <p class="font-medium text-slate-900">{{ calloutTitle }}</p>
      {% endif %}
      <p class="{% if calloutTitle %}mt-1{% endif %} text-sm text-slate-700">{{ calloutContent }}</p>
    </div>
  </div>
</div>
